# Docker CI CD

## Starting Jenkins

`docker pull jenkins/jenkins`

`docker run -t -name jenkins -p 8080:8080 -p 50000:50000 jenkins/jenkins`

localhost:8080

## CI

### Continuous Integration

Running the tests

## CD

### Continuous Delivery

build docker image

run docker container
